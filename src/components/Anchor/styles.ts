import styled from 'styled-components';

export const StyledAnchor = styled.a`
  color: inherit;
  text-decoration: none;
  cursor: pointer;

  &:hover,
  &:focus,
  &:active {
    text-decoration: none;
  }
`;
